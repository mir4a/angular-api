.test_page
	| Hello on test page
	.test_wrap
		ul.users_list
			li(ng-repeat="user in test.users", ng-class-odd="'red'")
				| {{ user.first_name }} {{ user.last_name }} — {{ user.email }}
				button(ng-click="test.getInfo(user)")
					| Get info

		br
		form(name="message")
			// FIXME: Debounce didn't work seems like form scope is wrong
			textarea(ng-model="text", ng-keyup="cancel($event)", name="text", ng-model-options="{ debounce: 1000 }")
		pre(ng-bind="text")
		pre {{ text }}